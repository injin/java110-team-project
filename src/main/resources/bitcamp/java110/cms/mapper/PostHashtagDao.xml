<?xml version="1.0" encoding="UTF-8"?>
<!-- PhotoDao 클래스가 사용할 SQL이 들어 있는 파일 -->
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bitcamp.java110.cms.dao.PostHashtagDao">

    <insert id="insert" parameterType="map"
            useGeneratedKeys="true" keyColumn="tagno" >
        insert into mv_post_tag(cont, pstno)
        values(#{cont},#{pstno})
    </insert>
    
    <select id="get" parameterType="String" resultType="java.util.List">
       select mp.cont, mpt.pstno
       from mv_post_tag mpt, mv_post mp
       where mpt.pstno = mp.pstno;
    </select>
    
    <delete id="delete" parameterType="int">
       
    </delete>
    
</mapper>