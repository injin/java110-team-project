<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bitcamp.java110.cms.dao.MemberDao">

    <resultMap type="member" id="memberMap">
        <id     column="mno"      property="mno"/>
        <result column="admin"    property="admin" />
        <result column="mid"      property="id" />
        <result column="nick"     property="nickname" />
        <result column="p_phot"   property="profileImage" />
        <result column="c_phot"   property="coverImage" />
        <result column="pr"       property="pr" />
        <result column="gend"     property="gender" />
        <result column="age"      property="age" />
    </resultMap>
    
    <insert id="insert" parameterType="member"
            useGeneratedKeys="true" keyColumn="mno" keyProperty="mno">
        insert into mv_memb(admin,mid,nick,p_phot,c_phot,pr,gend,age)
        values(if(#{admin},'Y','N'),#{id},#{nickname},#{profileImage},#{coverImage},#{pr},#{gender},#{age})
    </insert>
    
    <select id="findById" resultMap="memberMap" parameterType="string">
        select mno,
               (admin = 'Y') admin,
               mid,
               nick,
               p_phot,
               c_phot,
               pr,
               gend,
               age
         from mv_memb
         where mid = #{id}
    </select>
    
<!--
    <update id="updateFirst" parameterType="String">
    
    </update>
-->
    <!-- 
    <update id="updateFirst" parameterType="String">
	   UPDATE mv_memb
	   <trim prefix="SET" suffixOverrides=",">
	      <if test="exam1 != null">exam1 = #{exam1},</if>
	      <if test="exam2 != null">exam2 = #{exam2},</if>
	   </trim>
	   WHERE
	      idx = #{idx}
	</update>
	 -->
	
</mapper>